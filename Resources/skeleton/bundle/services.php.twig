<?php

{% block use_statements %}
use Symfony\Component\DependencyInjection\Definition;
use Symfony\Component\DependencyInjection\Reference;
use Symfony\Component\DependencyInjection\Parameter;
{% endblock use_statements %}

/*

{% block services %}
$definition = new Definition('{{ namespace }}\Model\{{ blockname_lower}}block');
$definition->addTag('landing_page_field_type.block_type', array('alias' => '{{ blockname_lower }}block'));
$container->setDefinition(
    'blocks.{{ blockname_lower }}block', $definition
);
{% endblock services %}

*/
